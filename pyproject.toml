[tool.ruff]
line-length = 100

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true

[tool.vulture]
min_confidence = 80

[tool.bandit]
exclude_dirs = ["tests"]

[tool.pylint.main]
ignore = ["venv"]

[tool.pyupgrade]
keep-runtime-typing = true