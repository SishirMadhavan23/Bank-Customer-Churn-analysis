#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "📝 Running commit message validation..."

# You can add commit message linting here if needed
# Example: commitlint --edit $1

echo "✅ Commit message is valid! ✨"
